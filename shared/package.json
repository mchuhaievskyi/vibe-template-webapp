{
  "name": "@vibe-template/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:typespec": "tsp compile typespec/main.tsp --output-dir typespec-output",
    "clean": "rm -rf dist typespec-output"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.10.0",
    "@typespec/http": "^1.10.0",
    "@typespec/openapi3": "^1.10.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "vite": "8.0.3"
  }
}
